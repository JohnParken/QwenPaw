#!/usr/bin/env bash
# ==============================================================================
# QwenPaw 联调环境 - [全流程一键关闭文件] (macOS / Linux)
# 安全优雅关闭所有服务并释放端口 5179, 8088, 8089
# ==============================================================================
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
node "${SCRIPT_DIR}/scripts/manage-live.mjs" stop
