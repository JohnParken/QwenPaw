[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = --confcutdir=.
markers =
    native: requires real Seatbelt and QwenPaw runtime
    postgres: requires dedicated real PostgreSQL
